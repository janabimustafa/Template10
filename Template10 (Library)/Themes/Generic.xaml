<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Template10"
    xmlns:controls="using:Template10.Controls">
    <Style TargetType="controls:SplitViewButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:SplitViewButton">
                    <RadioButton x:Name="radioButton" Width="{TemplateBinding Width}" MinWidth="0"
                             MaxWidth="{TemplateBinding MaxWidth}"
                             HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                             Command="{TemplateBinding NavCommand}"
                             GroupName="Nav"
                             IsChecked="{TemplateBinding IsChecked}"
                             IsEnabled="{TemplateBinding  IsEnabled}"
                             Content="{TemplateBinding Content}"
                             Visibility="{TemplateBinding Visibility}">
                        <RadioButton.Style>
                            <Style TargetType="RadioButton">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{ThemeResource SystemControlForegroundBaseHighBrush}"/>
                                <Setter Property="Padding" Value="8,6,0,0"/>
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                <Setter Property="VerticalContentAlignment" Value="Top"/>
                                <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
                                <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
                                <Setter Property="MinWidth" Value="120"/>
                                <Setter Property="UseSystemFocusVisuals" Value="True"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="RadioButton">
                                            <Grid x:Name="RootGrid" Width="{TemplateBinding Width}"
                                  Background="Transparent">
                                                <Grid.Resources>
                                                    <SolidColorBrush x:Name="HamburgerBackground" Color="#FFD13438" />
                                                    <SolidColorBrush x:Name="HamburgerForeground" Color="White" />
                                                    <SolidColorBrush x:Name="NavButtonBackground" Color="#FFD13438" />
                                                    <SolidColorBrush x:Name="NavButtonForeground" Color="White" />
                                                    <SolidColorBrush x:Name="NavAreaBackground" Color="White" />
                                                    <SolidColorBrush x:Name="SecondaryBorder" Color="DimGray" />
                                                    <SolidColorBrush x:Name="NavButtonCheckedOverlayBackground" Color="#FF7D1F22" />
                                                    <SolidColorBrush x:Name="NavButtonHoverOverlayBackground" Color="#FF434343" />
                                                </Grid.Resources>
                                                <VisualStateManager.VisualStateGroups>
                                                    <VisualStateGroup x:Name="CommonStates">
                                                        <VisualState x:Name="Normal">
                                                            <Storyboard>
                                                                <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                                                            </Storyboard>
                                                        </VisualState>
                                                        <VisualState x:Name="PointerOver">
                                                            <VisualState.Setters>
                                                                <Setter Target="HoverOverlay.Visibility" Value="Visible" />
                                                            </VisualState.Setters>
                                                            <Storyboard>
                                                                <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                                                            </Storyboard>
                                                        </VisualState>
                                                        <VisualState x:Name="Pressed">
                                                            <VisualState.Setters>
                                                                <Setter Target="PressedOverlay.Visibility" Value="Visible" />
                                                            </VisualState.Setters>
                                                            <Storyboard>
                                                                <PointerDownThemeAnimation Storyboard.TargetName="RootGrid" />
                                                            </Storyboard>
                                                        </VisualState>
                                                        <VisualState x:Name="Disabled" />
                                                        <VisualState x:Name="Checked">
                                                            <VisualState.Setters>
                                                                <Setter Target="CheckedOverlay.Visibility" Value="Visible" />
                                                            </VisualState.Setters>
                                                            <Storyboard>
                                                                <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                                                            </Storyboard>
                                                        </VisualState>
                                                        <VisualState x:Name="CheckedPointerOver">
                                                            <VisualState.Setters>
                                                                <Setter Target="CheckedOverlay.Visibility" Value="Visible" />
                                                                <Setter Target="HoverOverlay.Visibility" Value="Visible" />
                                                            </VisualState.Setters>
                                                            <Storyboard>
                                                                <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                                                            </Storyboard>
                                                        </VisualState>
                                                        <VisualState x:Name="CheckedPressed">
                                                            <VisualState.Setters>
                                                                <Setter Target="CheckedOverlay.Visibility" Value="Visible" />
                                                                <Setter Target="PressedOverlay.Visibility" Value="Visible" />
                                                            </VisualState.Setters>
                                                            <Storyboard>
                                                                <PointerDownThemeAnimation Storyboard.TargetName="RootGrid" />
                                                            </Storyboard>
                                                        </VisualState>
                                                        <VisualState x:Name="CheckedDisabled">
                                                            <VisualState.Setters>
                                                                <Setter Target="CheckedOverlay.Visibility" Value="Visible" />
                                                            </VisualState.Setters>
                                                        </VisualState>
                                                        <VisualState x:Name="Indeterminate" />
                                                        <VisualState x:Name="IndeterminatePointerOver" />
                                                        <VisualState x:Name="IndeterminatePressed" />
                                                        <VisualState x:Name="IndeterminateDisabled" />
                                                    </VisualStateGroup>
                                                </VisualStateManager.VisualStateGroups>
                                                <Grid x:Name="CheckedOverlay" Visibility="Collapsed">
                                                    <Rectangle Fill="{ThemeResource NavButtonCheckedOverlayBackground}" />
                                                </Grid>
                                                <Rectangle x:Name="PressedOverlay" Fill="White" Opacity=".2" Visibility="Collapsed" />
                                                <Rectangle x:Name="HoverOverlay" Fill="{ThemeResource NavButtonHoverOverlayBackground}" Visibility="Collapsed" />
                                                <ContentPresenter x:Name="ContentPresenter" HorizontalAlignment="Left"
                                                  VerticalAlignment="Stretch" HorizontalContentAlignment="Left"
                                                  VerticalContentAlignment="Center" Background="Transparent"
                                                  Content="{TemplateBinding Content}" FontSize="16"
                                                  Foreground="{ThemeResource NavButtonForeground}" />
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </RadioButton.Style>
                        <!--<RadioButton.Template>
                            <ControlTemplate>
                                <ToggleButton MaxWidth="{Binding MaxWidth, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                          HorizontalAlignment="{Binding HorizontalAlignment, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                          Command="{Binding Command, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                          CommandParameter="{Binding CommandParameter, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                          Content="{Binding Tag, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                          IsChecked="{Binding IsChecked, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            -->
                        <!--<Setter Property="Height" Value="{TemplateBinding Height}" />
                                <Setter Property="MinWidth" Value="48" />-->
                        <!--
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ToggleButton">
                                                        <Grid x:Name="RootGrid" Width="{TemplateBinding Width}"
                                  Background="Transparent">
                                                            <Grid.Resources>
                                                                <SolidColorBrush x:Name="HamburgerBackground" Color="#FFD13438" />
                                                                <SolidColorBrush x:Name="HamburgerForeground" Color="White" />
                                                                <SolidColorBrush x:Name="NavButtonBackground" Color="#FFD13438" />
                                                                <SolidColorBrush x:Name="NavButtonForeground" Color="White" />
                                                                <SolidColorBrush x:Name="NavAreaBackground" Color="White" />
                                                                <SolidColorBrush x:Name="SecondaryBorder" Color="DimGray" />
                                                                <SolidColorBrush x:Name="NavButtonCheckedOverlayBackground" Color="#FF7D1F22" />
                                                                <SolidColorBrush x:Name="NavButtonHoverOverlayBackground" Color="#FF434343" />
                                                            </Grid.Resources>
                                                            <VisualStateManager.VisualStateGroups>
                                                                <VisualStateGroup x:Name="CommonStates">
                                                                    <VisualState x:Name="Normal">
                                                                        <Storyboard>
                                                                            <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                                                                        </Storyboard>
                                                                    </VisualState>
                                                                    <VisualState x:Name="PointerOver">
                                                                        <VisualState.Setters>
                                                                            <Setter Target="HoverOverlay.Visibility" Value="Visible" />
                                                                        </VisualState.Setters>
                                                                        <Storyboard>
                                                                            <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                                                                        </Storyboard>
                                                                    </VisualState>
                                                                    <VisualState x:Name="Pressed">
                                                                        <VisualState.Setters>
                                                                            <Setter Target="PressedOverlay.Visibility" Value="Visible" />
                                                                        </VisualState.Setters>
                                                                        <Storyboard>
                                                                            <PointerDownThemeAnimation Storyboard.TargetName="RootGrid" />
                                                                        </Storyboard>
                                                                    </VisualState>
                                                                    <VisualState x:Name="Disabled" />
                                                                    <VisualState x:Name="Checked">
                                                                        <VisualState.Setters>
                                                                            <Setter Target="CheckedOverlay.Visibility" Value="Visible" />
                                                                        </VisualState.Setters>
                                                                        <Storyboard>
                                                                            <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                                                                        </Storyboard>
                                                                    </VisualState>
                                                                    <VisualState x:Name="CheckedPointerOver">
                                                                        <VisualState.Setters>
                                                                            <Setter Target="CheckedOverlay.Visibility" Value="Visible" />
                                                                            <Setter Target="HoverOverlay.Visibility" Value="Visible" />
                                                                        </VisualState.Setters>
                                                                        <Storyboard>
                                                                            <PointerUpThemeAnimation Storyboard.TargetName="RootGrid" />
                                                                        </Storyboard>
                                                                    </VisualState>
                                                                    <VisualState x:Name="CheckedPressed">
                                                                        <VisualState.Setters>
                                                                            <Setter Target="CheckedOverlay.Visibility" Value="Visible" />
                                                                            <Setter Target="PressedOverlay.Visibility" Value="Visible" />
                                                                        </VisualState.Setters>
                                                                        <Storyboard>
                                                                            <PointerDownThemeAnimation Storyboard.TargetName="RootGrid" />
                                                                        </Storyboard>
                                                                    </VisualState>
                                                                    <VisualState x:Name="CheckedDisabled">
                                                                        <VisualState.Setters>
                                                                            <Setter Target="CheckedOverlay.Visibility" Value="Visible" />
                                                                        </VisualState.Setters>
                                                                    </VisualState>
                                                                    <VisualState x:Name="Indeterminate" />
                                                                    <VisualState x:Name="IndeterminatePointerOver" />
                                                                    <VisualState x:Name="IndeterminatePressed" />
                                                                    <VisualState x:Name="IndeterminateDisabled" />
                                                                </VisualStateGroup>
                                                            </VisualStateManager.VisualStateGroups>
                                                            <Grid x:Name="CheckedOverlay" Visibility="Collapsed">
                                                                <Rectangle Fill="{ThemeResource NavButtonCheckedOverlayBackground}" />
                                                            </Grid>
                                                            <Rectangle x:Name="PressedOverlay" Fill="White" Opacity=".2" Visibility="Collapsed" />
                                                            <Rectangle x:Name="HoverOverlay" Fill="{ThemeResource NavButtonHoverOverlayBackground}" Visibility="Collapsed" />
                                                            <ContentPresenter x:Name="ContentPresenter" HorizontalAlignment="Left"
                                                  VerticalAlignment="Stretch" HorizontalContentAlignment="Left"
                                                  VerticalContentAlignment="Center" Background="Transparent"
                                                  Content="{TemplateBinding Content}" FontSize="16"
                                                  Foreground="{ThemeResource NavButtonForeground}" />
                                                        </Grid>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="UseSystemFocusVisuals" Value="True" />
                                        </Style>
                                    </ToggleButton.Style>
                                </ToggleButton>
                            </ControlTemplate>
                        </RadioButton.Template>-->
                    </RadioButton>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
